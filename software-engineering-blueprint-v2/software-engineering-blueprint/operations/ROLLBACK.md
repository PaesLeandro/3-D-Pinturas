# Rollback

For risky releases document:
- rollback trigger;
- application rollback;
- migration compatibility;
- feature-flag strategy if used;
- data repair implications;
- verification after rollback.

Prefer backward-compatible migrations that permit application rollback.
